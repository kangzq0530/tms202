/**
 * 公會聯盟負責人
 */
var status = -1;
var choice;
var guildName;
var partymembers;

function start() {
    action(1, 0, 0);
}

function action(mode, type, selection) {
    cm.say("還沒完成");
    cm.dispose();
}